# 2026-07-10 对内开发更新

## 今日代码与逻辑层面修改说明

今日按需求隐藏协作相关的可见入口，仅处理导航层，不删除或重构协作功能代码。新版 BrandAI 侧边栏中的「成员协作」入口仍保留在导航常量中，但增加 `hidden` 标记，并在侧边栏渲染时过滤；旧 workspace 导航中的「团队成员」入口从导航列表中移除。对应的成员协作页面、API、权限校验、审核/审批相关代码均未删除，后续如需恢复入口，只需要恢复导航展示即可。

同时补齐每日发布文档结构，继续沿用 `docs/daily-release/internal/` 与 `docs/daily-release/external/` 两个目录，分别存放对内开发说明和对外用户更新说明。

## 验证情况

- 已确认 BrandAI 新侧边栏不会渲染 `hidden` 的「成员协作」导航项。
- 已确认旧 workspace 导航不再包含 `/members` 的「团队成员」入口。
- 本次仅隐藏入口，不影响 `/members` 页面和 `/api/workspaces/[wsId]/members` 相关实现。

## 关联信息

- 类型：体验入口隐藏
- 范围：导航展示层
- 风险：低，功能代码保留，可快速恢复入口
