# 主业务流程在页面上的体现不清晰

- **日期**:2026-06-24
- **归类**:O｜可见性·验收缺口(叠 S｜需求没想清楚)
- **质量**:good

## 原始提问
> 我认为现在主业务流程在页面上的体现，它有一些不清晰。这个做完之后，我们同步把验收的内容也放进去。

## 追问澄清(Q&A)
> 本条为建表时的示例种子,澄清按事后补记。
- Q(定位):主流程指哪段? → A:工作台出图闭环(生成 → 选变体 → 改图 → 终选 → 导出)。
- Q(现象):不清晰具体指? → A:后端已闭环(phase-1 6/6 真验收),但页面上看不出"现在走到主流程哪一步"。
- Q(期望调整):想变成什么样? → A:页面上能直接看到主流程的当前阶段;并把"怎样算验收通过"的内容一并放上去。

## 三要素(澄清后)
- 定位:`/workspace` 工作台 + 主流程跨页表达。
- 现象:功能在、但主流程的阶段/进度/验收口径在页面上不可见。
- 期望调整:页面上显式体现主流程阶段 + 同步放入验收(DoD)内容。

## 归因
- 根因(一句):原始需求没写死"主流程在页上如何算清晰/算验收通过"(DoD 缺位)→ 功能在但不可见。
- 基础设施动作:CDS 云预览里直接看主流程 + 视觉快照固化「主流程清晰可见」;DoD 与需求同时写(前置,不是"做完后补")。
- 目标/下一步:① 工作台/主流程阶段在页面显式化;② 验收内容前置进 spec 模板。
