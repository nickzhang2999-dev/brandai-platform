# BrandAI 验收报告（视觉 + 工程）· 2026-06-22

> 验收对象：分支 `claude/beautiful-dirac-6lszxf`（commit `7ad9ae1`，与 `main` HEAD 一致 / PR #27 已合并）
> 验收范围：对照 `docs/01–08` 产品方案清单 + `README.md` 进度表（137 行声称全绿），核验「是否全部完成、是否可测试」，并做视觉/灰度验收。
> 验收人：Claude Code（remote CDS session）

---

## 0 · 结论速览

| 维度 | 结论 |
|---|---|
| **产品清单覆盖（docs 01–08）** | ✅ 完整。README 进度表是 docs 的严格超集，doc02 全部 P0/P1 模块均有对应实现行，全部标 ✅ |
| **代码级可测试性** | ✅ 通过（前提：按 `CLAUDE.md §1` 先 `pnpm db:generate`）。L1 契约/UI 测试、AI pytest、typecheck、build 全绿 |
| **⚠️ 复现陷阱** | 全新检出**未先 `pnpm db:generate`** 时，typecheck 与 build 会**红**（Prisma client 未生成）。非代码 bug，但「可测试」依赖该前置步骤 |
| **视觉/灰度验收** | 🟡 部分：部署健康 + 边缘门禁可验；**认证态 5 页 + 真出图取证需超管密码**（本会话不可得，且 `AUTH_ALLOW_DEMO=0`） |
| **真 provider 取证** | 🟡 env 层为 `mock`（真 provider 配置若存在，按设计在加密 `AppSetting` DB 中，env 兜底）；无认证/DB 访问无法现场取证 |

---

## 1 · 产品清单 ↔ 实现 逐项核对（docs/02 模块 → README 行）

doc01§9 左侧导航 = 6 项（首页 / Campaign / 品牌知识库 / 素材库 / 模板库 / 工作台）→ 代码 `lib/brandai-mock.ts::navItems` 实有 6 主项 + `members`(成员协作, phase-2)，**一致**。

| 页面 | doc02 模块 | README 对应 | 状态 |
|---|---|---|---|
| P01 首页 | M01–M08（导航/用户/通知/问候/AI输入/快捷/近期Campaign/推荐品牌） | A1–A3 · B1–B5 | ✅ 全覆盖 |
| P02 Campaign | M01–M12（标题/搜索/阶段/品牌/时间/排序/卡片/状态/进度/AI摘要/快捷操作） | C1–C10 | ✅ 全覆盖 |
| P03 品牌知识库 | M01–M12（AI共创/快捷词/上传/Logo/色彩/字体/语调/视觉/设计/预览/摘要） | D1–D14 | ✅ 全覆盖（+recognize/parse-manual 额外） |
| P04 素材库 | M01–M17（标题/上传/文件夹/搜索/类型/统计/网格/详情/AI标签/AI描述/加入项目/设为参考） | E1–E13 | ✅ 全覆盖 |
| P05 工作台 | M01–M15（路径/工具栏/大图/变体/提示词/风格词/品牌约束/参考/提交制作） | F1–F16 | ✅ 全覆盖（+多尺寸/改图/终选/导出） |
| 通用 | 弹窗体系 / AI输入规则 / 卡片 | H1–H16 | ✅ 全覆盖 |
| 业务闭环 | doc01§10 端到端流程 | J1–J8 | ✅ 全覆盖 |

> doc02 表中跳号（如 P04 的 M09/M10/M11/M15、P05 的 M05/M07/M09/M11/M14）为源文档本身留空，非实现遗漏。
> doc01§11「一期不纳入」：多团队协作 / 多级审批 / 完整计费 / 视频 / PSD / 泛网络抓取。README 中协作(G6 I25/K2)与审阅流为 phase-2 **超前实现**（加性，不违范围）；真计费 enforcement UI 按 §11 留 phase-2。**与产品方案范围无冲突**。

**结论：docs 01–08 产品清单的全部一期页面/模块在代码层均已落地，README 进度表逐项对齐、无缺口行。**

---

## 2 · 代码级可测试性（工程验收门）

环境：Node v22.22.2 / pnpm 9.15.0 / Python 3.11.15。在干净检出执行：

| 检查 | 命令 | 结果 |
|---|---|---|
| L1 契约 + UI 测试 | `pnpm test` | ✅ 通过 |
| AI 服务单测 | `apps/ai` `pytest` | ✅ **93 passed** |
| 类型检查 | `pnpm -F web typecheck` | ✅ 通过（`db:generate` 后） |
| 生产构建 | `pnpm -F web build` | ✅ 通过（`db:generate` 后） |

### ⚠️ 复现陷阱（必须记录）
干净检出**直接**跑 `typecheck`/`build`（未先 `pnpm db:generate`）会**失败**：
- `typecheck`：`recognize.worker.ts:94 — Namespace 'Prisma' has no exported member 'InputJsonValue'`
- `build`：`@prisma/client did not initialize yet ... /api/admin/activity`

根因：Prisma client 未生成。执行 `CLAUDE.md §1` 规定的 `pnpm db:generate` 后两者立即转绿。
**对「是否可测试」的判断**：可测试，但 README「测试」段落应显式把 `pnpm db:generate` 列为 `typecheck`/`build` 的前置（当前 §1 本地起栈有，但「测试 + typecheck」小节没强调），否则接手者首跑会误判为红。

---

## 3 · 视觉 / 灰度验收

灰度分支：`brandai-platform-claude-beautiful-dirac-6lszxf`（已注册 + 部署，commit 7ad9ae1）。

### 3.1 部署健康（实测通过 ✅）
```
GET /api/health → {"web":"ok","ai":"ok","worker":{"worker":"ok","count":7}}
```
web / ai（内部网） / worker（7 进程）三服务全 ok。

### 3.2 边缘门禁 + 入口页（非认证态，实测通过 ✅）
| 检查 | 期望 | 实测 |
|---|---|---|
| `GET /` 未登录 | 307 → /login | ✅ `307 → /login?callbackUrl=%2F` |
| `GET /campaigns·/brand-knowledge·/assets·/workspace·/templates` 未登录 | 全部 307 → /login | ✅ 5 页全部 `307 → /login?callbackUrl=...` |
| `GET /api/admin/users` 未授权 | 401/拒绝 | ✅ `401` |
| `GET /login` | 200 + 紫色品牌渲染 | ✅ `200`，91KB，含 `BrandAI / 品牌 / 登录` |

→ 验证 `I3 边缘门禁中间件`、`I2/I4 admin 接口鉴权`、登录入口页紫色视觉渲染均真实生效。


### 验收限制（如实声明）
1. **认证态视觉验收受阻**：边缘门禁中间件要求所有非公开路由持会话 cookie；`AUTH_ALLOW_DEMO=0`（免密登录已按 phase-1 安全收尾关闭），且本会话**无超管密码**（`inernoro@gmail.com`）。因此 5 个业务页的**认证态渲染 + 真出图取证无法现场完成**。
2. **真 provider 取证受阻**：CDS 项目 env 层 `IMAGE_PROVIDER=mock`/`VLM_PROVIDER=mock`（API key 空）。按 `CLAUDE.md §0.2 / settings.ts`，真 provider 配置加密存 `AppSetting`（DB 优先于 env）；无认证/DB 访问无法读取并现场出真图。README 记录的真出图验收（`gpt-image-2`，2026-06-21）依赖 DB 内配置，本轮无法复验。
3. **跨分支隔离配置注意**：本分支 CDS env 继承项目级 `AI_SERVICE_URL=http://ai:8000`（裸别名），未见分支级 `BULLMQ_PREFIX`/`AI_SERVICE_URL=cds-<branchId>-ai:8000`。README §I28/隔离章节明确：共享 Redis/docker 网络下，裸别名会致 worker round-robin 命中**别分支** ai → AI 路由间歇 404。**新建分支灰度做真 AI 验收前，应先按 README 设分支级前缀/服务名**，否则 AI 文本/出图路由可能串台。

---

## 4 · 建议（给产品 / 工程）

1. **README「测试」小节补一行**：`typecheck`/`build` 前必须 `pnpm db:generate`，避免接手者误判红。
2. **真出图视觉验收**：如需本轮完成，请提供超管密码（或临时开 `AUTH_ALLOW_DEMO=1` 灰度专用账号），我可即跑「登录 → 5 页 200 → 建 Campaign → 真出图(gpt-image-2) → 改图 → 终选 → 导出 ZIP」全链路取证并补本报告 §3。
3. **跨分支隔离自动化**：将 `BULLMQ_PREFIX` / 分支级 `AI_SERVICE_URL` 纳入 `cds-compose.yml`（README 已列为 phase-2），免每分支手设。

---

*报告由自动化验收流程生成，事实源：`docs/01–08`、`README.md` 进度表、`CLAUDE.md`、灰度 CDS API 实测。*
