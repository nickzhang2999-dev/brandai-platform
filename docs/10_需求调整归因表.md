# BrandAI｜需求调整归因表 v0.2

> **本表的本质是归因，不是问题清单。** 把编码 AI 每次的"需求调整提议"归到一个固定类别，**聚合后看分布**；分布直接告诉我们该把基础设施的钱投在哪。
>
> 两个原则：
> 1. **不冤枉 AI** —— 责任列把"需求没写清"和"AI 没做到"分开。
> 2. **不拒绝提议** —— 每一类都有去向（backlog / DoD 前置 / CI 回归 / CDS 预览 / 技术债清单），而不是简单的 yes/no。
>
> 与 `README.md` 顶部进度表互补：进度表记「做没做」，本表记「提议从哪来」。

---

## 一、六个类别（N / S / I / O / R / E）

| 代号 | 类别 | 一句话 | 不冤枉谁 | 根在哪个产物 |
|---|---|---|---|---|
| **N** | 新增需求 | 原始范围里真没有 | 谁都不怪，是新范围 | 产品 backlog |
| **S** | 需求没想清楚 | 需求在，但留了歧义/缺口 | 不怪 AI，怪 spec | `docs/` PRD、字段文档 |
| **I** | 没按需求做 | spec 写清了，AI 没交付 | 这才是 AI 的实现缺口 | 代码 / 契约 |
| **O** | **可见性·验收缺口** | **做了，但验收方在页面上看不到/确认不了** | **不怪 AI，怪没有验收手段** | **CDS 预览 + 视觉测试** |
| **R** | 优化·重构提议 | 既非 bug 也非范围，AI 主动建议改进 | 中性，是判断题 | 技术债清单 |
| **E** | **环境·部署·运行时·验收方法** | **代码/需求都对，但线上跑旧 commit / 缓存盖住 / worker 未就绪 / 测错环境** | **不怪 AI 也不怪测试者，怪上线判据缺失** | **部署流水线 / 运行时配置 / `cds-deploy-verify`** |

> **为什么从 5 类扩到 6 类（v0.2 加 E）**：用真实测试者反馈压测时发现，最常见的争议是「这到底是 bug 还是没部署/缓存？」——代码取证显示代码正确、仅线上复现（典型如线上创建失效）。这既不是 I（代码没做到），也不是 O（做了但看不清）——**O 的隐含前提是代码已正确上线**。它属于运行时/部署/验收姿势，N/S/I/O/R 全装不下；硬塞进 I 就**冤枉 AI**、污染分布。这正是本仓库的头号失败模式（CLAUDE.md §0 规则4「部署成功 ≠ 新代码在跑」、`cds-deploy-verify` 技能都在防它）。
>
> **判 E 的纪律**：E 不能靠静态代码盖棺——必须**运行时取证**（`/api/health` + `.next/CDS_DEPLOY_COMMIT` 比对 HEAD + 一个反映新行为的端点冒烟，见 `cds-deploy-verify`）。确证前先标「疑似 E」，证实是部署/缓存才归 E，证实是代码缺陷才归 I。

---

## 二、类别 → 该投的基础设施（决策表）

提议集中在哪一类，就投哪一格：

| 类别集中 | 说明 | 该投的基础设施 | 降验收成本? |
|---|---|---|---|
| **N 多** | AI 在帮你发现范围 | 把 backlog 流程跑顺即可，不是 infra 问题 | 否（但别拒绝） |
| **S 多** | spec 模板缺"完成定义" | **验收标准（DoD）和需求同时写** —— 前置，不是"做完再补" | 是，前置最省 |
| **I 多** | 回归/交付没卡住 | 契约测试 + 视觉回归 + CDS 冒烟，在合并前抓 | 是 |
| **O 多** | 功能在但不可见 | **CDS 云预览 + 视觉化自动测试**（主投资线） | 是，最直接 |
| **R 多** | AI 想重构 | 单独技术债清单，批量审，别混进验收 | 中性 |
| **E 多** | "是 bug 还是没部署"反复扯皮 | **上线判据自动化**：部署后 health + `CDS_DEPLOY_COMMIT` 指纹比对 + 新行为冒烟（`cds-deploy-verify`）；缓存破除 | 是，省下扯皮成本 |

---

## 三、归因日志（逐条记录·长期维护）

| # | 日期 | AI 原话/提议 | 类别 | 根因（一句） | 基础设施动作 | 质量 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | 2026-06-24 | [主业务流程在页面上的体现不清晰](./prd-intake/2026-06-24-主流程页面可见性.md) | **O**（叠 S） | 原始需求没写死"主流程在页上如何算清晰"（DoD 缺位）→ 功能在但不可见 | CDS 预览里直接看主流程 + 视觉快照固化 | good | 待办 |
| 2 | 2026-06-24 | "做完后把验收内容也放进去" | **S** | 验收标准当初没和需求一起落；且说的是"做完**后**补"，应前置到"和需求同时写" | DoD 模板前置 | good | 待办 |
| 3 | 2026-06-24 | 部署验收时发现：一条 type=logo 的 FORBIDDEN 品牌规则（实为正向留白指引「Logo 周围保留不少于 1x 高度的安全留白」）会**无条件硬阻断该品牌的所有出图**（`ai-constraints.ts` 把 imagery/graphic/logo 三类 FORBIDDEN 一律当硬阻断，不区分"禁止项"与"指引项"） | **S**（叠 O） | 规则强度模型没想清楚：logo 类 FORBIDDEN 既可能是真禁止（竞品 logo/违规图形→该拦），也可能是指引（安全留白→不该拦全部）；系统无法区分，用户把指引设成 FORBIDDEN 即锁死整个品牌出图。叠 O：422 给了原因，但页面没引导"降级这条规则即可解锁" | **已根治 2026-06-24**：`HARD_BLOCK_RULE_TYPES` 去掉 `logo`，只留 imagery/graphic（真·内容禁令）；logo 类降为建议（仍折入 promptAdditions/negativePrompt 不丢指引）。真禁止走 ProhibitionRule。补 2 个回归用例（logo 指引不拦 + imagery 仍拦）。**灰度真验**：规则保持 FORBIDDEN 不动数据，同一出图直接成功 | good | ✅ 已修 |
| 4 | 2026-06-25 | V0.02 评审：`getConfirmedRules` 默认排序被从 `createdAt asc` 翻成 `updatedAt desc`，只为知识库页"最新优先"，却牵连规则快照冻结、合规校验、导出、硬阻断门等 8 个调用方 | **R**（叠 S） | 单页展示偏好改了 `lib/` 共享函数的默认行为，爆炸半径越界；快照/合规依赖确定性排序 | **已收窄 2026-06-25**：默认恢复 `createdAt asc`（确定性），新增显式 `{ order: "recency" }` 仅出图路径（generate worker / brand-preview）opt-in。AGENTS.md §3 立规"共享函数别为单页改默认" | good | ✅ 已修 |
| 5 | 2026-06-25 | V0.02 评审：多品牌切换只存 `localStorage`、不进 cookie/URL → "当前品牌"双事实源（服务端 layout 一套、客户端 context 一套），刷新闪烁、深链/分享带不上品牌 | **S** | "会被刷新/分享的状态必须服务端权威"在 spec 没写死；且该能力本属 phase-2（多租户），提前落地未补归因 | **已改服务端权威 2026-06-25**：活动品牌落 `ACTIVE_BRAND_COOKIE`（`lib/brand-cookie.ts`），`getOrCreateActiveBrand` 读 cookie 前先过 `Membership` 校验（防 IDOR），切换后 `router.refresh()` 同步 SSR。CLAUDE.md §0.7 + AGENTS.md G-AUTHORITATIVE 立规 | good | ✅ 已修 |
| 6 | 2026-06-25 | V0.02 交付文档第 7 节自述"前端 typecheck 未完成 / 预览未完成"，第 9 节同时勾"已提交并推送" | **I** | spec（CLAUDE.md §1/§4）写清 push 前必须全绿，未按门禁交付；"沙箱无网"被当放行理由 | CLAUDE.md §0.7 + AGENTS.md G-VERIFY 立规"未验证不交付，无网不是放行理由"；后续把门禁挂 CI 合并前强制 | good | 待办（流程·待 CI 强制） |
| 7 | 2026-06-27 | 用户：把 prd_agent 视觉创作的「画布(可缩放/平移) + 局部重画等操作概念」搬进 BrandAI AI 工作台 | **N** | 一期工作台只有单图预览 + 按钮缩放 + 改图卡；画布手势与局部重画(蒙版 inpaint)是 prd_agent 成熟能力的跨项目迁移，属新增范围 | 顺 backlog：本轮落地画布 wheel 手势(F2) + 选中图浮动操作条 + 局部重画 INPAINT 全链路(F19，复用现有 /edit→worker→ai.edit 管线 + Pillow 蒙版归一)；真·蒙版出图待灰度真验（接 `cds-deploy-verify`） | good | 本轮代码完成（待灰度真验真·蒙版出图） |
| 8 | 2026-06-28 | 用户连续反馈画布"点按钮工具条消失/反复抖动/点操作就卡住/双击改文字进不去，前端问题数不清" | **I**（叠 O） | 4 个交互 bug（指针冒泡清选择 / `router.replace` RSC 软导航环 / 操作即发+禁用整条无出口 / `setPointerCapture` 吞 `dblclick`+`autoFocus` 被 in-flight click blur+点 div 不 blur input）**全部通过 typecheck/build/L1**——交付门里没有「真浏览器点交互」这一层，编译绿被当成了完成。叠 O：用户自己测才暴露 | **已根治 2026-06-28**：四类全修（见 `docs/11` §A）；立 **AGENTS.md G-INTERACT 门**（画布/交互 UI 必须本地 Chromium 直连 localhost 逐功能点过；沙箱 MITM 代理关外网预览域、localhost 在 noProxy）；沉淀可复用 harness `tests/interaction/canvas-functions.mjs`（20/21 真测通过，唯一未过=本地无对象存储的上传）；加 `data-testid=canvas-item` 锚点 | good | ✅ 已修 + 立门 |
| 9 | 2026-07-05 | 用户反馈 V0.0.7 工作台选择素材「必须 100% 调用」后，实际出图没有把素材呈现在图上 | **O**（叠 I） | V0.0.7 只验证 `STRICT` 参数被记录和进入提示词，没有验收 provider 请求是否真实携带图像输入；功能在 UI 上可见但真实效果不可确认。叠 I：代码最终仍走纯文生图 `/images/generations`，未走图像输入 | **V0.0.8 已修**：`ReferenceImage.mode=STRICT` 后 AI 服务改走 `provider.edit()` 图像输入链路；Web/API 双层限制当前仅 1 张 STRICT；测试断言 STRICT 调用 `edit()` 且不调用 `generate()` | good | ✅ 已修 |
| 10 | 2026-07-07 | 用户重新定义平台图片只有素材库、模板库、生成图三类，并要求素材调用以水印方式真实出现在结果图上 | **N**（叠 O） | 「素材要被使用」与「参考图只参考风格」此前混在同一 reference 语义里，导致 UI 和生成链路都容易把必须出现的素材交给模型自由理解。叠 O：V0.0.8 虽走图像输入，但仍不是位置/透明度/大小可控的确定性合成 | **V0.0.9 本轮改造**：新增 `Asset.libraryKind` 三分法；素材库只列 `MATERIAL`，模板库只列 `TEMPLATE`，生成图镜像为 `GENERATED`；工作台拆「素材（水印使用）」和「参考图（模板库）」；worker 用 `sharp` 对 `watermarkOverlays` 做服务端确定性叠加，模板图仅进 AI inspiration | good | ✅ 代码完成（待 CDS 视觉归档） |
| 11 | 2026-07-08 | 用户确认「生成图」作为左侧入口后，指出未来会沉淀大量图片，需要新增查询入口，并按项目筛选维度过滤图片 | **N**（叠 O） | V0.0.9 只补齐了生成图入口和三分法，但页面仍是按时间堆图；分类存在但缺少检索、过滤和项目上下文，后续量大后难以查找和复盘 | **V0.10 本轮改造**：新增 `GeneratedAsset` 契约与 `generated-assets` 专用接口；生成图页支持搜索、项目筛选、项目状态筛选、时间筛选、排序；卡片显示项目上下文并可回跳工作台 | good | ✅ 代码完成（待门禁与 PR） |
| 12 | 2026-07-08 | 用户要求 AI 工作台编辑图片时，如果已选择水印，最终编辑生成图也必须按规则添加水印 | **I**（叠 O） | V0.0.9 只把水印合成接入普通生成 worker，编辑 worker 没有接收和应用 `watermarkOverlays`；用户在同一个工作台选择水印后，编辑结果却不带水印，属于同一规则在不同链路表现不一致。叠 O：界面显示水印素材，用户自然预期所有最终图都应用 | **V0.0.11 本轮修复**：`EditVersionInput` 新增 `watermarkOverlays`；工作台 `runEdit()` 提交当前水印配置；`edit.worker` 在 AI edit 底图返回后调用 `applyWatermarksToImage()`，再上传、落库、镜像为 `GENERATED` | good | ✅ 代码完成（待门禁与 PR） |

| 9 | 2026-07-07 | 用户在 /admin/users 看到订阅额度列却发现"额度不能改"，要给设计师开额度、把默认 STARTER 从 5/日 提到 30/日,并要"一个更改面板" | **O**（叠 N） | 配额 enforcement（K1/I17）早已落地并真跑，但**只有 seed/迁移里的数字、没有任何 UI 能改**——做了却在页面上碰不到、改不了（典型 O：可见性·操作缺口）。叠 N：可编辑套餐额度的管理面板本属 phase-2 计费 UI 范围 | **本轮补** `/admin/plans` 内联编辑面板 + `api/admin/plans[/tier]`（改各档 日/月/品牌上限,即时生效,-1=不限）；STARTER 默认经 seed + `20260707000000_starter_quota_bump` 迁移提到 30/日·600/月（仅当仍是旧默认才改,不覆盖已定制）。gate 全绿 + 新增 `admin-plan.test.ts` 契约测试 | good | ✅ 已补 |
| 11 | 2026-07-07 | 用户：工作台参考图选「100% 调用」(如 logo) 后,该素材没被用在生成图里 | **I** | 参考图 STRICT/INSPIRATION 的使用模式只走到「文字提示」:真 `gpt-image-2` 的 generate 用 `/images/generations`(文生图不吃输入图)+`_build_body` 对 openai 丢 extra→参考图像素从未进模型,STRICT 与 INSPIRATION 真出图行为相同。契约里区分了模式,但出图管线没落实「100% 调用」的语义,是实现缺口 | **已修**:ReferenceImage 加 `mode`,worker 透传;apps/ai 新增 `generate_with_references()` 把 STRICT 参考图字节经 `/images/edits` 送模型(logo 原样落图),仅 STRICT 走图生图、INSPIRATION 维持文字。Codex P1 SSRF 一并修(WEBSITE 参考图按 K7 `allow_private_initial=False`)。gate 全绿(L1 142+L2 99)+ 灰度部署健康;STRICT logo eyeball 待鉴权真出图一次确认 | good | ✅ 已修(待真出图 eyeball) |
| 10 | 2026-07-07 | 用户：黑夜模式下登录/管理设置/用户管理三页白卡不可读；皮肤四选只想留白天黑夜；进了管理页回不了首页；管理页太简陋要下滑、要页签；管理员设置和个人设置同页是否安全/会被篡改 | **I**（叠 O） | 黑夜白卡=`CreamCard` `dark:bg-cream/text-ink` 反模式（暗色渲染近白卡、内部 token 却翻黑），过 typecheck/build 但真机不可读——又一次「编译绿≠能用」（同 #8 类）。Mono/Tech 皮肤在 `styles.css` 无 token 块，选中静默回退 light（做一半的 O）。管理页无返回入口 + 长页下滑=可见性/导航缺口（O）。安全其实早已服务端门禁齐全（每页 `isAdminUser` 重定向 + `requireAdmin` + 密钥加密掩码），属"看不出安不安全"的 O | **本轮修**：CreamCard 改语义 token 一处覆盖全站；皮肤删到白天/黑夜；admin 迁到独立 `(admin)` 组壳（返回首页按钮 + 6 段页签 + `/admin` 索引重定向）；侧栏个人齿轮(`/account`)与管理入口(`/admin`,仅 admin)分离。gate 全绿。**建议投基础设施**：暗色是视觉回归重灾区，接 `cds-deploy-verify` 时加暗/亮双主题视觉快照（同 #1/#8 的 O 去向） | good | ✅ 已修 |
| 13 | 2026-07-12 | 用户要求 V0.0.12 补齐图生图、局部修改、整图修改、扩图、素材调用三档、水印拖拽、工作台草稿保存、账号昵称修改 | **N**（叠 S/O） | 原工作台以文字生图为主，虽然已有编辑链路，但“基于上一版继续迭代”的产品路径不够显性；“调用”语义从 0~100 改为只参考/绝对调用/适配调用，需要契约和 UI 同步定型。叠 O：水印位置只靠 X/Y 滑杆，用户难以确认落点；离开工作台草稿丢失，已填写内容不可见也不可恢复 | **V0.0.12 本轮改造**：工作台新增文字生图/整图修改/局部修改/扩图四模式；扩图支持方向和范围；素材调用分为只参考(模板库)、绝对调用(确定性叠加，不改内容，可手动缩放定位)、适配调用(出现且可按品牌色调整)；素材可拖入画布；水印配置可直接拖拽定位和拖拽缩放；工作台草稿按品牌本地恢复；账号设置支持修改昵称并驱动首页「你好，XXX」 | good | ✅ 已完成（门禁与真浏览器通过） |
| 14 | 2026-07-16 | 用户要求第一阶段先修正工作台右下角队列、误显示“AI 正在生成…”，并重新明确品牌套件→项目→工作台层级；同时提出后续要做 Lovart 式品牌套件 PDF 自动解析 | **O**（叠 S/N） | 队列浮层和“空状态误判生成中”都是技术状态直接暴露给用户，造成用户以为系统在自动审图/生成。品牌套件、项目、工作台、生成图之间的层级表达不够清楚，素材库/模板库与品牌套件归属关系也容易混淆。Lovart 式 PDF 自动解析属于新增品牌套件能力，范围大，需拆成可验收阶段推进 | **第一阶段处理**：移除用户端队列浮层；生成中只在服务端明确 PENDING/RUNNING 时展示；侧边栏与创建入口统一“品牌套件”文案；生成图页说明归属于当前品牌套件并按项目筛选；品牌套件页新增“一次上传构建品牌套件”入口，PDF 直传后进入 parse-manual，图片按六个槽位进入 recognize，草稿规则由用户确认启用，证据缩略图支持点击预览。**后续阶段待设计**：从 PDF 自动切出 Logo/颜色/字体/图像等原始素材并批量归类确认、落专门品牌套件素材上限 | good | ✅ 第一阶段完成 |
| 15 | 2026-07-17 | 用户要求 AI 工作台整体参考 Lovart 画布习惯：画布面积更大，素材库可直接放入画布，本地上传/拖入/粘贴要更直接；模板库只进入参考上下文，水印配置暂时取消，右侧保留历史对话和输入 | **N**（叠 O） | 原工作台仍像“表单 + 小画布”，素材、水印、模板、生成历史分散在不同区域，用户做图时需要频繁找入口；素材真实业务语义是可被摆放的画布元素，而不是单独调 X/Y 的水印配置。叠 O：已有画布底座支持拖拽/选择/缩放，但入口没有组织成用户自然使用路径 | **V0.0.14 画布改造第一阶段**：画布区域扩大；新增底部工具坞（素材、模板、上传、粘贴、文字、方形）；素材库选择后进入画布托盘，可点击或拖拽落图；本地图片支持拖入画布、上传和剪贴板粘贴；模板库只作为参考图上下文传给 AI，不直接落画布；右侧新增历史对话入口；旧水印配置入口和隐藏水印提交参数撤下 | good | ✅ V0.0.14 第一阶段完成（待真浏览器完整交互栈复验） |
| 16 | 2026-07-17 | 用户要求 V0.0.15 继续收敛 Lovart 式体验：工作台右侧删除制图模式/卖点/场景/尺寸/数量等表单，只保留历史过程与底部输入框；隐藏前端用户不需要看到的 CDS 技术条；品牌套件要按图片化资产展示，取消 PDF 20MB 限制并上传后自动解析 | **O**（叠 N） | V0.0.14 已完成画布底座，但右侧仍残留传统表单，和“对话驱动做图”的目标冲突；CDS 预览状态条属于后台技术信息，暴露给用户会干扰验收；品牌套件规则卡仍偏文字化，用户上传 logo/图像后看不到 Lovart 式资产陈列。叠 N：PDF 不限大小与上传即解析属于新的导入体验要求 | **V0.0.15 验收版**：工作台右侧改为上方历史对话/过程、下方输入框；移除右侧制作模式、需求卖点、场景、生成数量、多渠道尺寸等配置区；新增 CDS 技术预览条前端隐藏保护；品牌套件内容卡新增 logo/图像/字体图片预览、颜色色块预览；品牌套件 PDF 前端 20MB 限制移除，上传后直接进入 parse-manual/recognize 解析链路 | good | ✅ 代码完成（待 V0.0.15 预览验收） |
| 17 | 2026-07-17 | 用户在 V0.0.15 预览工作台提交时遇到 “Project not found in this workspace”，需要修复 | **I**（叠 O） | URL 或本地草稿可能保留旧品牌/旧工作区项目 ID，页面展示会回退到第一个项目，但提交仍使用无效 projectId，导致后端拒绝；英文错误直接露出，用户无法判断处理方式。叠 O：项目归属同步属于后台状态，用户不应感知 | **V0.0.16 修复**：项目列表加载完成后校验当前 projectId 是否属于当前品牌套件，失效则自动切到当前品牌套件下可用项目并清理旧 gen/job 状态；提交出图时只使用 activeProjectId；Project not found 错误转为中文提示 | good | ✅ 代码完成（待 V0.0.16 预览验收） |
| 18 | 2026-07-17 | 用户明确品牌套件需对标 Lovart，并要求使用一份包含规范、图片和本地素材的 PDF 自动抓取全部品牌套件内容；采用项目顶部持久选择，不采用输入框 `@品牌套件` 单次约束 | **N**（叠 S/O） | N：从 PDF 文字解析升级为图文逐页识别、视觉资产裁切与六类自动归档，并让确认结果进入项目生成链路。S：明确两种应用方式只保留项目级持久应用。O：原第一阶段只有文字规则与 PDF 证据，容易让用户误判为已经完成深度提取 | **V0.0.17**：品牌套件首屏收敛为 3×2 六类入口 + 完整手册上传区，手动录入降为次级；PDF 最多 120 页逐页渲染，VLM 分批识别并合并六类草稿，选区自动裁切为 `BRAND_KIT` 资产并保留页码证据；确认后的规则、主 Logo 和图像参考自动进入当前品牌全部项目生成。2026-07-18 在 CDS 部署环境使用 101 页《一家一居VI全 预览版》完成真实浏览器验收：准确提取 8mm 最小尺寸、六个字体名称、三个标准色、版式/图像特征与两条品牌文案；六类规则全部启用，并由 GPT Image 2 按项目级约束生成 4 个真实变体 | good | ✅ 真实 PDF 解析、项目级应用与浏览器出图验收完成 |
| 19 | 2026-07-18 | 用户再次确认只保留项目级自动应用品牌套件，暂不支持输入框 `@品牌套件` 单次约束，并要求可直接看出当前项目是否已经应用品牌规范 | **O**（叠 S） | 工作台虽然服务端已经自动加载已确认规则，但界面没有展示应用状态，且英文占位符仍提示输入 `@`，与已确认产品范围冲突 | 工作台项目路径增加“品牌套件自动应用 · N 条规则”状态；输入框改为纯生成描述提示并移除 `@` 引导。真实浏览器验证 R9 项目显示自动应用 6 条规则，生成链路完成后沉淀 1 次历史出图、4 个变体 | good | ✅ 已完成并真实验收 |
| 20 | 2026-07-18 | 用户在 V0.0.17 的空白品牌套件上传同一份 101 页《一家一居VI全 预览版》时出现 `AI /v1/parse-manual failed: 500`，要求在同一版本彻底修复并达到 R9 效果 | **I**（叠 O） | 运行日志显示相邻请求一成一败，排除文件和上传问题。长 PDF 被拆成 13 个视觉批次；原实现只对带“rate limit”字样的 5xx 降级，任一批次遇到网关裸 500/502/503/504 或网络超时，就会向上抛错并抹掉整份手册已经成功的页面结果。浏览器中的 NextAuth 红色浮层另由旧开发 chunk 的四小时缓存造成，属于伴随错误而非 PDF 500 根因 | 批次级容错扩展到网关裸 5xx 与网络异常：在 provider 内部重试耗尽后，保留已渲染页面和精确 PDF 文字继续合并六类规则，并向用户返回具体降级页码警告；401/403/400 等配置错误仍保持失败。解析器版本提升为 `grounded-six-slot-r6`，强制 worker 只连接部署了本次修复的 AI 容器；新增裸 500 回归测试。部署后在原失败品牌“测试 0717”重新上传同一文件，成功生成并启用六类规则；新建“测试0717-品牌约束验收”项目后，工作台显示自动应用 6 条规则 | good | ✅ 同文件真实解析、六类结果与项目级应用复验完成 |
| 21 | 2026-07-18 | 用户提供 Lovart 空白态、完成态视频与截图，要求品牌套件只调整权限和页面交互：空白态只保留开始引导；完成态不再混入创建态；删除品牌预览；“我的品牌套件”支持新建、改名、删除；Logo/图片在当前页面弹出预览；仍合并在 `codex/brandai-v0.0.17`，不得影响主分支 | **O**（叠 I/N） | 原 V0.0.17 同时堆叠页面总标题、六类创建入口、内容卡、AI 摘要和品牌预览，空/完成两种任务状态互相冲突；品牌切换仍是下拉框，真实 workspace 已有 PATCH/DELETE 权限接口却没有 Lovart 式管理入口；证据图片使用 `target=_blank`，与原位查看目标不符。叠 I：旧页面没有按已给目标状态完整收敛。叠 N：新增套件卡片管理与灯箱缩放/下载交互 | **V0.0.17 UI 收敛**：空白/完成互斥渲染；完成态采用紧凑手册导入条 + 设计/品牌指南 + Logo/颜色/字体/图像素材架；移除页面手动文字共创、快捷提示、AI 摘要和品牌预览；路由侧栏改“我的品牌套件”卡片列表，OWNER 可新建/改名/删除；图片改当前页灯箱预览、缩放和下载。PDF worker、解析算法、规则确认、项目级约束均不修改 | good | ✅ 四项门禁、指定分支推送及 PR #50 已完成；CDS Web 服务待恢复后执行真浏览器验收 |
| 22 | 2026-07-18 | `codex/brandai-v0.0.17` 重部署后预览域名返回 Worker 健康 JSON；CDS 诊断显示 Web 启动链的 `prisma db push` 因准备删除仍有 6 条数据的 `Generation.chatContext` 而退出，要求安全修复且不得丢数据 | **E**（叠 R） | CDS 历史 Web profile 仍调用根级 `pnpm db:push`，与仓库现行 `scripts/web-startup.sh` 的 `prisma migrate deploy` 安全启动策略漂移；直接加 `--accept-data-loss` 会删除业务数据。叠 R：根脚本名称被灰度历史配置长期引用，不能只改 CDS 当前实例 | **安全兼容修复**：新增 `db:deploy`，根级 `db:push` 兼容入口转发到 `prisma migrate deploy`；包内 `push` 保留给本地 throwaway 数据库。CDS 重部署不再做破坏性 schema 对齐，遗留列与 6 条数据均保留 | good | 🔧 已修复，待门禁与 CDS 重部署验收 |

> **填表约定**：AI 每提一次需求调整，自带一个代号（见 §一）+ 一句根因。本表只做归档与聚合，不做 yes/no 裁决。
>
> **统计口径（关系到 §四看分布准不准）**：① 每行**只按主代号计一次**；`(叠 X)` 是注脚，次因**不计入**分布。② `质量=draft`（未澄清）的行**不计入**分布，仅留痕。
>
> **入口 = `/prd` 技能**：用户抛一句感受（如 `/prd 我觉得流程太卡了`）→ 技能走「好问题/差问题」闸门追问到三要素具体 → 单条事实记录落 `docs/prd-intake/`（见该目录 README），并回填本表一行（「AI 原话/提议」列链接到记录）。本表是汇总索引，`prd-intake/` 是逐条原始事实。

---

## 四、复盘节奏

每隔一批提议看一次分布：

- **O / S 占多** → 加码 CDS 预览 + 视觉测试 + DoD 模板（主投资线被验证）。
- **I 占多** → 加码 CI 回归（契约 + 视觉回归）。
- **N 占多** → 是好事，顺 backlog 即可。
- **R 占多** → 收进技术债清单批量审。
- **E 占多** → 加码上线判据自动化（`cds-deploy-verify` 进 CI/部署后必跑）+ 缓存破除；同时校准验收方的测试姿势（确认测的是新 commit）。

> **集合型吐槽**：一条输入若是"问题很多/到处都是/根本没法用"（如 `连基本测试都过不了`），它不是一条需求而是**多条的集合**。`/prd` 技能的「拆解闸门」会先拆成多条、各跑三要素、各落一行；拆不动的残留部分落 `draft` 不计入分布。**严禁把集合硬塞成一行**（会把不同代号混算，污染统计）。
